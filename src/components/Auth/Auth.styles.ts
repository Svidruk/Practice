import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  container: {
    display: 'flex',
    padding: 36,
    alignItems: 'center',
    position: 'relative',
  },
  closeBtn: {
    position: 'absolute',
    right: 40,
    top: 40,
  },
  img: {
    display: 'block',
    width: 743,
  },
  formContainer: {
    padding: '0px 170px',
  },
});
