import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  container: {
    display: 'grid',
    padding: 36,
    position: 'relative',
    gridTemplateColumns: '1fr 1fr',
  },
});
