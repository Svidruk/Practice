export interface ProductDescriptionData {
  id: number;
  imgUrl: string;
  name: string;
  price: number;
  quantity: number;
  soldAmount?: number;
  description: string;
  extraInformation: string;
}
